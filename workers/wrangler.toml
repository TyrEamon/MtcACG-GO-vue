name = "mtcacg-worker"
# ✅ 修改：文件在根目录，不是 src/ 目录
main = "index.js"
compatibility_date = "2024-01-01"

# D1 数据库绑定
[[d1_databases]]
binding = "DB"  # ⚠️ 不能改，代码中写死了
database_name = "myblove"   # ← 需要填写实际的数据库名
database_id = "18c655b1-fab6-4548-9b8f-072982ba195f"     # ← 需要填写实际的数据库 ID

# 环境变量（可选）
# 注意：BOT_TOKEN 应该用 `wrangler secret put BOT_TOKEN` 配置
# 而不是写在这里（避免泄露）
[vars]
# 如果有其他非敏感的环境变量，可以写在这里
# EXAMPLE_VAR = "value"
